<div class="panel panel-primary">
  <!-- Default panel contents -->
  <div class="panel-heading">{{results.listName}}</div>
  <div class="panel-body">
    <p>You are ranked {{results.currentUser_Rank}} out of {{results.totalPeople}} people and you have {{results.currentUser_PointsLeft}} votes left. </p>
  </div>
<table class="table table-hover">
  <thead>
  <tr>
    <th>Rank</th>
    <th>Name</th>
    <th>Points</th>
    <th>Vote!</th>
  </tr>
  </thead>
  <tbody id="list_table" list_id={{results.listId}} > 
      {{#each results.users}}
      <tr id="list_item_{{@index}}" points={{userPoints}}  user_to_id={{user.id}}>
        <td>{{userRank}}</td>
        <td class="item_name"><p>{{user.name}}
          {{#if mostHatedD }}<span class="glyphicon glyphicon-star"></span>{{/if}}
          {{#if mostHatedW }}<span class="glyphicon glyphicon-thumbs-down"></span>{{/if}}
          {{#if mostHatedM }}<span class="glyphicon glyphicon-hand-down"></span>{{/if}}
        </p>
        </td>
        {{#if votesThisWeek}}
        <td class="item_points"><strong>{{userPoints}}</strong>  <small>(<span class="glyphicon glyphicon-arrow-up"></span>+{{votesThisWeek}} this week)</small></td>
        {{else}}
        <td class="item_points"><strong>{{userPoints}}</strong>  <small>(<span class="glyphicon glyphicon-arrow-right"></span>No change this week)</small></td>
        {{/if}}
        <td>
          <div class="hidden add_point_div">
            <select class='display_points_to_vote'>
              {{{formatAvailablePoints}}}
            </select>

            <button class="btn btn-danger valid_vote" votesThisWeek={{votesThisWeek}} >Vote!</button>
          </div>
          <div class="add_point_button">
            <button class="btn display_vote" pointsLeft = {{currentUser_PointsLeft}}>+</button>
            <span class="alert message fade in"></span>
          </div> 
        </td>
      </tr>
      {{/each}}
  </tbody>
</table>

</div>